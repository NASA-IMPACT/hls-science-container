[tool.pytest.ini_options]
addopts = "--ignore=src/hls-nextgen-orchestration/tests/benchmarks"

[tool.ruff]
line-length = 88
target-version = "py312"
include = ["packages/**/*.py", "packages/**/*.pyi"]

[tool.ruff.lint]
select = ["F", "E", "W", "I", "UP"]
ignore = ["E501", "E731"]

[tool.ruff.lint.isort]
known-first-party = [
  "hls_utilities",
  "hls_browse_imagery",
]

[tool.mypy]
strict = true
python_version = "3.13"
namespace_packages = true
explicit_package_bases = true

[[tool.mypy.overrides]]
module = [
  "tests.*",
  "*.tests.*"
]
strict = false
disallow_untyped_defs = false
disallow_incomplete_defs = false
check_untyped_defs = true
warn_return_any = false

# The Rust LaSRC package (lasrc-rs / `lasrc`) is a linux-64-only conda package
# absent from the dev/typecheck environment; the orchestration tasks import it
# lazily at runtime only.
[[tool.mypy.overrides]]
module = ["lasrc", "lasrc.*"]
ignore_missing_imports = true

[tool.pixi.workspace]
name = "hls-science-container"
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tool.pixi.pypi-dependencies]
hls-science-container = { path = ".", editable = true }
