[workspace]
channels = ["conda-forge"]
name = "lasrc"
platforms = ["linux-64"]
version = "0.1.0"
preview = ["pixi-build"]

[tasks]

[dependencies]
espa-product-formatter = { path = "packages/espa-product-formatter" }
